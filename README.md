# 🖥️ C++ Winsock Multi-threaded Chat Server

이 프로젝트는 **C++와 Winsock2**를 사용하여 밑바닥부터 설계한 **멀티스레드 채팅 서버**입니다.
Qt GUI 클라이언트와 통신하며, 안정적인 데이터 처리와 객체 지향적 설계를 목표로 개발했습니다.

## 🛠️ Key Features (핵심 기술)

### 1. Multi-threading & Synchronization (스레드 안전성)
- `std::thread`를 사용하여 다중 클라이언트 접속을 동시에 처리합니다.
- `std::mutex`와 `lock_guard`를 사용하여 **Critical Section(임계 영역)**을 보호, 여러 명이 동시에 채팅을 쳐도 데이터가 꼬이지 않도록 설계했습니다.

### 2. ClientHandler Architecture (객체 지향 설계)
- `ClientHandler` 클래스를 통해 각 클라이언트의 소켓, IP, 닉네임, 현재 방(Room) 상태를 **캡슐화(Encapsulation)**하여 관리합니다.
- 코드가 길어지는 것을 방지하고 유지보수성을 높였습니다.

### 3. Room Management System (방 관리 로직)
- `std::map<string, vector<ClientHandler*>>` 자료구조를 활용하여 효율적인 방 생성 및 입장/퇴장 로직을 구현했습니다.
- `/join`, `/rooms` 명령어를 통해 동적으로 방을 오갈 수 있습니다.

### 4. Admin Console & Command Pattern
- 서버 관리자가 콘솔에서 직접 명령어를 입력하여 공지(`notice`)를 띄우거나, 악성 유저를 강퇴(`kick`)할 수 있는 관리자 시스템을 구축했습니다.
- 클라이언트 명령어(`/whisper`, `/list` 등)를 파싱하여 처리하는 커맨드 패턴을 적용했습니다.

## ⚙️ Tech Stack
- **Language:** C++ (Standard 17/20)
- **Network:** Winsock2 API (TCP/IP)
- **Design Pattern:** Singleton Pattern (Server Instance), Command Pattern
